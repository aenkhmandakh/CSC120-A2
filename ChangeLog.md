Resubmit 5/8/2026

Changes:

- Changed buy() sell() updateOS() and refurbish() instead of inputting the parameters to create a new computer inside buy, it just takes in a computer object

- Changed sell() updateprice() and refurbish() to actually measure the length of the inventory and make sure it is not empty

- changed